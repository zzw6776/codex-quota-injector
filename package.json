{
  "name": "codex-quota-injector",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Inject a standalone multi-account quota, expiry and account-switching panel into the Codex macOS client.",
  "scripts": {
    "doctor": "node src/cli.mjs doctor",
    "read-quota": "node src/cli.mjs read-quota",
    "inject": "node src/cli.mjs inject",
    "launch": "node src/launcher.mjs",
    "preview": "node src/preview-server.mjs",
    "build:sea": "node scripts/build-sea.mjs",
    "package:macos": "node scripts/package-macos.mjs",
    "package:windows": "node scripts/package-windows.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "esbuild": "0.28.1",
    "postject": "1.0.0-alpha.6",
    "resedit": "3.0.2"
  }
}

# 2026-09-16 两周变更复查

## 范围与执行边界

按用户要求，先将已有改动升级到 0.1.253，提交并推送 `2add068`，再审查最近两周的变更。审查基线为 `d13ad8cd9b9a6b51283783da82f73227134cc610`，截至 `2add068` 共 23 次提交、165 个变更文件。

围绕账号迁移与凭据刷新、模型管理与能力探测、Responses/Chat 转换、Relay 与宿主健康、Widget 更新、用量记录、Windows/WSL 配置恢复、生命周期监督、测试编排和打包工作流检查调用链。第一轮定位并修复明确失效机制，第二轮复核修改、调用方和新增回归用例；未再确认需要继续修复的高价值问题。这是代码审查结论，不代表真实平台验收或保证没有缺陷。

钥匙串命令失败可能在错误文本中带出凭据的问题，用户明确要求不处理，本次未修改。未因风格、纯理论风险或低收益边界增加兼容分支。

## 本次修复

| 问题 | 明确触发条件与影响 | 修复及回归覆盖 |
| --- | --- | --- |
| 图片能力误判 | 图片请求遇到认证失败、限流或服务故障，错误文本包含 image 等词时，原逻辑将其保存成“不支持图片” | 复用已有失败分类，只有明确的不支持类响应才能形成负面能力结论；增加 Responses/Chat 的 401、403、429、500、503 回归用例，保留原有明确拒绝和图片内容校验 |
| Windows 恢复覆盖用户配置 | 启停恢复测试在切换运行环境时已合并 Codex 新增配置，恢复前文件没有再次变化，匹配 lastAppliedSha256 后错误恢复整份旧备份 | 已合并外部内容的文件继续走字段级恢复及运行方式所有权检查；用例覆盖原配置存在/不存在、多次切换及重复恢复 |
| 真实模型测试后台中断误报成功 | 测试子进程被信号结束，exit code 为 null，被 Number(null) 转成 0，可能继续后续阶段或标记通过 | 复用测试子进程退出处理函数，信号终止明确失败；增加真实 Node 子进程被 SIGTERM 中断的回归用例 |
| WSL 使用旧通过报告 | 同一批次复用固定清单文件名，WSL 在生成新清单前退出时，宿主可能读到上次 passed | 每次执行使用唯一清单；校验源码、运行环境，以及通过清单对应的进程退出码和全部阶段结果；增加缺失本次清单、旧源码、漏阶段和失败退出的用例 |
| 模型列表漏更新 | 模型发现与账号/网络等状态同时变化，完整 Widget 更新提前消费发现结果，后续 DOM 更新误以为已经处理 | 让局部页面更新只消费一次发现结果；浏览器用例覆盖同时更新、模型数量变化、原表单节点与未保存 Key 保留，以及提交的选中模型 |

这些修复沿用现有状态和处理边界，没有新增服务或供应商专用兼容路径。Windows 修复定位于项目自身配置恢复逻辑，不涉及绕过官方平台策略。

## 版本与生效

- 工作区修复版：0.1.254，package.json 与 package-lock.json 同步。
- Widget：156 → 157，原因是模型发现的页面更新行为发生变化。
- 模型能力检测缓存：13 → 14，旧版可能保存错误的图片负面结论，因此旧结果需显示“检测规则已更新”。后续重新检测会产生模型请求，不能未经当次授权自动执行。
- Relay 协议保持 78；本次未改变运行时请求转换、路由或协议结构。

本轮只读查询确认日常客户端仍为正式版 0.1.252、phase=ready。正式包使用内嵌代码，开发环境的 Widget 热加载入口不能替换该正式包。本轮未安装、接管或重启，不能把工作区修复视为当前客户端已经生效。

## 验证状态

- 修复和复查期间未运行自动化测试，也未启动免费回归、官方模型测试、DeepSeek Flash 测试或启停恢复测试。
- 对本次变更执行 JavaScript 语法检查、JSON 解析及版本一致性检查、git diff --check；这些静态检查不等于回归用例通过。
- Windows 配置恢复用例归 Windows 原生组件；本机 macOS 不执行 Windows/WSL 实机验收。
- 新增用例已经写入项目，执行结果待用户主动触发测试。此前版本的免费回归、真实模型测试和启停恢复测试结论不继承到本次源码。

0.1.253 快照已推送；本次 0.1.254 审查修复保留在工作区，尚未提交或推送。

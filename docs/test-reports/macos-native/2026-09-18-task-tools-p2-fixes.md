# 任务工具两个 P2 修复与重新审查

- 日期：2026-09-18；macOS 原生 arm64，Node v26.7.0，官方 CLI 0.155.0-alpha.9。
- 范围：刷新超时/错误与任务就绪通知交错；首次四项探针排队时的目录诊断隔离。属于免费回归的公共及 macOS 原生定向子集，不是完整启停恢复验收。
- 源码：应用 0.1.284、Widget 174、Relay 87、健康 Schema 4。健康字段和请求结构未改变，内部 Schema 版本不递增；中继行为改变，协议 86 升至 87。
- 最后日常核对：正式版 0.1.284 / Relay 86，当前任务健康 ready；本次修复尚未部署。模型请求 0，日常重启 0。

## 已关闭问题

1. 配置刷新开始时仅失效旧连接证据一次。刷新超时或错误只记录刷新结果，保留本次收到的任务启动通知及检查结果。超时释放刷新等待后立即继续派发队列，不必依赖迟到响应或再次点击。迟到成功不得重复派发或恢复旧连接证明。
2. scoped 目录观察始终只更新 diagnostic，不再以 checks 是否存在为条件。删除旧的目录结果写主状态、取消启动宽限计时器及借用启动通知补 runtimeStatus 的路径。无作用域目录不描述任何任务连接，健康追踪器直接忽略；目录异常不能改写主状态或阻止首次探针。

## 验证与证据复用

| 验证 | 原始结果 | 最终结果 |
| --- | --- | --- |
| 六个新增交叉场景，修复前 | 1/6 通过，5 个失败，确认失效机制 | 修复后 6/6 通过 |
| health / host-tools / app-server-relay / lifecycle-host / lifecycle-task-tools | 定向批次 84/85 通过，1 个失败 | 修正目录诊断夹具的明确 connected 状态后，仅重跑失败项 1/1；所选 85 项最终通过 |
| 同版本官方 app-server，shim 原生协议 | 1/1 通过 | 已覆盖任务就绪、四项真实协议调用与全局/scoped 列表；无模型请求 |
| git diff --check | 通过 | 通过 |

执行：`node --test test/host-tools.test.mjs test/host-health.test.mjs test/app-server-relay.test.mjs test/lifecycle-host.test.mjs test/lifecycle-task-tools.test.mjs`；失败项定向重跑使用 `--test-name-pattern='目录只更新诊断'`。原生验证限定 `runtime-tests/host-health.test.mjs` 的 `官方 shim` 用例，`CODEX_TEST_CLI` 指向已确认的官方具体路径，并使用 `--test-concurrency=1`。

修复后首次 health/host-tools 验证曾为 30/33；三项保留旧“目录写主状态”预期的测试改为覆盖诊断独立及真实调用证据。随后唯一失败源于 runtimeStatus=null 的缓存目录被测试预期成 connected；新规则不会借用启动通知填补该状态，故夹具明确提供 connected。仅修改该测试夹具后复测其用例，生产输入没有改变；其他 84 项直接复用。所有原始失败保留，未使用上游接受项消除。

页面代码未变，复用既有 Widget 174 真实浏览器 4/4 的对应行为证据；未重新执行 31 秒主页停留、完整免费、付费模型或启停控制器，也不推定 Windows/WSL 原生通过。

## 重新审查

已核对超时前后就绪、迟到成功、错误响应、刷新前证据失效、探针限流排队、目录为空/缺工具/超时、任务隔离、历史门禁消费者及实际响应透传。目录修改任务主状态的旧路径已删除，刷新结果清空本次连接证据的调用也已移除。本次修复范围未发现新增 P1/P2 或阻断项。

既知可选 P3 保留：同连接的旧目录诊断超时后，迟到响应仍可能覆盖更新的目录诊断；它不能再修改任务主状态。此项不在本次两个 P2 的修复范围内。

私有输入清单与执行汇总：`.runtime/task-tools-p2-fixes-20260918/summary.json`（工具输出整理，不是原始 stdout 全文）；SHA-256：`f81694fa224d0e8cefc604b0a3d07fba7a8c54ec813c1509a222983517e714f6`。保留两个失效机制的修复前后证据及本次重新审查结论；部署后的生效核验另行留证。

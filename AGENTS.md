# 项目规则

## 开发与操作

- 使用中文，优先 CLI。修改前完整阅读直接相关代码，按证据确认根因；必要时反编译依赖。
- 行为变更须补充有意义的自动化测试，缺陷修复须覆盖真实失效机制；无法自动化的部分验证可分离契约并说明人工确认范围，不堆叠低价值测试。
- 可热加载的变更完成后直接重载对应页面或运行时，并核对实际版本/行为；失败时说明仍在运行的版本，不能把源码修改当成已生效。
- 探测版本只能执行已确认的 CLI 具体路径；GUI 主程序用文件元数据或已有日志识别，禁止批量执行通配匹配的 `.exe --version`。
- 项目使用 `never` 审批策略，审批弹窗不是测试项。禁止轮询、持续等待或 watch CI/CD；触发后提供页面链接。

## 复测与验收

- **只复测实际受影响的组件/用例和原生环境，复用未受影响的历史结果。** 按执行输入及实际运行时证据判断；发布版本、说明文档或无关用例变化不要求全量复测。旧报告缺少输入清单先审核差异，证据不足记待审核，不自动判失败、未执行或强制重跑。正式包的版本、构建、签名和资源独立核验。
- **已确认且符合证据复用条件的官方上游、环境兼容性和已接受的同类已知问题，计为验收通过。** 保留真实通过数和原始失败/阻断状态，另存接受项、证据及是否重新对照；项目回归、普通遗漏和未确认问题不能套用。
- 免费报告不是后台、桌面或启停测试的前置门禁；缺失、失败、过期均不要求先重跑免费回归。测试期间受测输入变化、供应商隔离、运行环境核对和预算/失败停止条件仍须遵守。
- 后台与桌面、不同模型、不同原生环境分别留证；桌面只等待对应后台通过。跨发布版本可按输入等价复用，跨平台/原生环境不能互相代替。六类测试的名称、计数单位与矩阵统一使用[测试总索引](docs/testing-index.md)。

## 测试授权与调度

- 常规修改只做必要的定向验证，不自动运行完整免费回归、真实模型或启停测试。用户要求测试后按已授权范围直接执行，不重复询问模型用量或专用短历史测试任务授权。
- “完整非重启测试”包括当前平台完整免费回归及官方模型、DeepSeek Flash 各自后台和桌面测试；已授权的组件满足条件即继续，不扩大供应商、平台或重启范围。只要求免费或局部补测时不扩展范围。
- 执行前展示计划；`--plan` 不发送模型请求或重启。真实模型执行由代理传入 `--confirm-token-use`。DeepSeek 只使用模型管理中已启用、当前检测有效的 `deepseek-flash` 预设，隔离其他模型；每批只绑定一个原生环境，不接受付费 `--runtime=all`，不自动切换桌面运行方式。
- 授权范围内的官方与 DeepSeek 后台默认并行，每个桌面组件只等待自己的后台；仅实际争用焦点、键鼠或可变配置的步骤串行，说明争用范围。独立标签页不等于焦点隔离，详见[并行调度](docs/testing-desktop-host.md#并行调度)。
- 启停恢复最后执行，须取得本次明确重启授权；用户排除重启时不再询问。任何安装接管、关闭或重连前，必须等待活动回合终态稳定落盘并核验历史投影，不能靠固定延时。禁止重复调度运行中的控制器；失败保留现场，不回滚、不备份旧安装。
- 启停前打开独立实时报告，使用平台监督器。Windows 完整启停自动覆盖 Windows/WSL 并恢复测试负责的运行方式字段，保留其他并发写入；字段被外部改写、配置删除或所有权不明时拒绝覆盖。历史修复须停止相关进程、备份原 rollout 和 SQLite，只修复可证明内容不变的已知结构问题；启动后只读核验投影追平。

## 异常归因

- 桌面、app-server、动态工具或 Windows 异常先查[已知问题](docs/desktop-known-issues.md)，再核对 rollout/SQLite 等证据。匹配时复用归因，不因换模型、新任务或项目版本号重复完整定位、付费测试或重启。
- 首次出现或不匹配时，用同版本官方 app-server、同一数据和原生环境做无 Relay/正式 Relay 最小对照；记录实际二进制版本，旧任务 `session_meta` 不能代替本轮版本。确认项目改变结果或其他证据定位项目代码后才修复；官方同类失败不加项目绕过，无法归因保持待确认。
- `read_thread` 正确任务的 completed 回合出现空 `items`，先读[分页问题说明](docs/read-thread-pagination-investigation.md)，不得删除 Relay 分页游标规避。WSL 分页与旧 Windows cwd 导致 node_repl ENOENT 已有对照，适用条件见已知问题表，不能扩大为 WSL 普遍不支持。

## 按需阅读与交付

开展对应操作前读取相关文档，不要求每个任务加载全部手册：

| 当前操作 | 必读文档 |
| --- | --- |
| 确定复测范围、查询结果或执行测试 | [测试总索引](docs/testing-index.md)：影响判断、组件/环境、命令入口 |
| 免费/后台测试、预算与隔离、启停恢复 | [测试说明](docs/testing.md)：对应章节；启停须读“报告和以后怎么防止漏测”及平台历史保护说明 |
| 桌面验收、条件能力与工具证据 | [桌面测试](docs/testing-desktop-host.md)：固定判据、并行、真实工具清单与同回合留证 |
| 归档或清理报告 | [报告归档](docs/test-reports/README.md)：按组件/环境/架构保留最近 5 次，关键证据额外保留 |
| 改动契约、缓存或准备提交/发布 | [版本规则](docs/versioning.md)：应用、Widget、Relay 与内部结构版本 |

每次获授权的测试结束后归档脱敏摘要并更新索引；原始私有材料留 `.runtime/`。每次提交前升级应用版本并同步 `package.json`、`package-lock.json`，默认补丁递增；仅文档修改不升级 Widget/Relay。主规则保留长期约束，操作细节维护在对应文档，历史结果不得作为新增执行门禁。
